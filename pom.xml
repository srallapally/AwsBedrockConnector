<!-- pom.xml -->
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0
                             https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>org.forgerock.openicf.connectors</groupId>
    <artifactId>awsbedrock-connector</artifactId>
    <version>1.5.20.29</version>
    <packaging>bundle</packaging>


    <properties>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <maven.compiler.source>17</maven.compiler.source>
        <maven.compiler.target>17</maven.compiler.target>

        <!-- Align all AWS SDK v2 modules via BOM -->
        <aws.sdk.version>2.39.5</aws.sdk.version>
        <jackson.version>2.16.1</jackson.version>
        <!-- Framework -->
        <openicf.framework.version>1.5.20.33</openicf.framework.version>
        <!-- Felix bundle plugin version -->
        <maven.bundle.plugin.version>5.1.9</maven.bundle.plugin.version>
    </properties>
    <dependencyManagement>
        <dependencies>
            <!-- AWS SDK v2 BOM -->
            <dependency>
                <groupId>software.amazon.awssdk</groupId>
                <artifactId>bom</artifactId>
                <version>${aws.sdk.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <dependencies>
        <!-- OpenICF framework — provided by the runtime (RCS / IDM) -->
        <dependency>
            <groupId>org.forgerock.openicf.framework</groupId>
            <artifactId>connector-framework</artifactId>
            <version>${openicf.framework.version}</version>
            <scope>provided</scope>
        </dependency>
        <dependency>
            <groupId>org.forgerock.openicf.framework</groupId>
            <artifactId>connector-framework-internal</artifactId>
            <version>${openicf.framework.version}</version>
            <scope>provided</scope>
        </dependency>

        <!--
            SLF4J API: provided by the ICF/OSGi container.
            Pinned here to prevent the SDK's transitive slf4j-api
            from landing on a different classloader.
        -->
        <dependency>
            <groupId>org.slf4j</groupId>
            <artifactId>slf4j-api</artifactId>
            <version>1.7.36</version>
            <scope>provided</scope>
        </dependency>

        <!--
            AWS SDK modules (versions come from BOM above).
            Exclude netty-nio-client and apache-client from service modules
            that pull them as default HTTP transports — we use
            url-connection-client instead.
        -->
        <dependency>
            <groupId>software.amazon.awssdk</groupId>
            <artifactId>iam</artifactId>
            <exclusions>
                <exclusion>
                    <groupId>software.amazon.awssdk</groupId>
                    <artifactId>netty-nio-client</artifactId>
                </exclusion>
                <exclusion>
                    <groupId>software.amazon.awssdk</groupId>
                    <artifactId>apache-client</artifactId>
                </exclusion>
            </exclusions>
        </dependency>
        <dependency>
            <groupId>software.amazon.awssdk</groupId>
            <artifactId>url-connection-client</artifactId>
        </dependency>
        <dependency>
            <groupId>software.amazon.awssdk</groupId>
            <artifactId>bedrock</artifactId>
            <exclusions>
                <exclusion>
                    <groupId>software.amazon.awssdk</groupId>
                    <artifactId>netty-nio-client</artifactId>
                </exclusion>
                <exclusion>
                    <groupId>software.amazon.awssdk</groupId>
                    <artifactId>apache-client</artifactId>
                </exclusion>
            </exclusions>
        </dependency>
        <dependency>
            <groupId>software.amazon.awssdk</groupId>
            <artifactId>bedrockagent</artifactId>
            <exclusions>
                <exclusion>
                    <groupId>software.amazon.awssdk</groupId>
                    <artifactId>netty-nio-client</artifactId>
                </exclusion>
                <exclusion>
                    <groupId>software.amazon.awssdk</groupId>
                    <artifactId>apache-client</artifactId>
                </exclusion>
            </exclusions>
        </dependency>
        <dependency>
            <groupId>software.amazon.awssdk</groupId>
            <artifactId>auth</artifactId>
        </dependency>
        <dependency>
            <groupId>software.amazon.awssdk</groupId>
            <artifactId>regions</artifactId>
        </dependency>
        <dependency>
            <groupId>software.amazon.awssdk</groupId>
            <artifactId>sdk-core</artifactId>
        </dependency>
        <dependency>
            <groupId>software.amazon.awssdk</groupId>
            <artifactId>aws-json-protocol</artifactId>
        </dependency>
        <dependency>
            <groupId>software.amazon.awssdk</groupId>
            <artifactId>s3</artifactId>
            <exclusions>
                <exclusion>
                    <groupId>software.amazon.awssdk</groupId>
                    <artifactId>netty-nio-client</artifactId>
                </exclusion>
                <exclusion>
                    <groupId>software.amazon.awssdk</groupId>
                    <artifactId>apache-client</artifactId>
                </exclusion>
            </exclusions>
        </dependency>

        <!--
            AWS SDK internals: formerly transitive, now declared explicitly
            so Embed-Transitive=false can find them. Versions from BOM.
        -->
        <dependency>
            <groupId>software.amazon.awssdk</groupId>
            <artifactId>aws-core</artifactId>
        </dependency>
        <dependency>
            <groupId>software.amazon.awssdk</groupId>
            <artifactId>aws-query-protocol</artifactId>
        </dependency>
        <dependency>
            <groupId>software.amazon.awssdk</groupId>
            <artifactId>aws-xml-protocol</artifactId>
        </dependency>
        <dependency>
            <groupId>software.amazon.awssdk</groupId>
            <artifactId>protocol-core</artifactId>
        </dependency>
        <dependency>
            <groupId>software.amazon.awssdk</groupId>
            <artifactId>http-auth</artifactId>
        </dependency>
        <dependency>
            <groupId>software.amazon.awssdk</groupId>
            <artifactId>http-auth-aws</artifactId>
        </dependency>
        <dependency>
            <groupId>software.amazon.awssdk</groupId>
            <artifactId>http-auth-spi</artifactId>
        </dependency>
        <dependency>
            <groupId>software.amazon.awssdk</groupId>
            <artifactId>http-auth-aws-eventstream</artifactId>
        </dependency>
        <dependency>
            <groupId>software.amazon.awssdk</groupId>
            <artifactId>http-client-spi</artifactId>
        </dependency>
        <dependency>
            <groupId>software.amazon.awssdk</groupId>
            <artifactId>identity-spi</artifactId>
        </dependency>
        <dependency>
            <groupId>software.amazon.awssdk</groupId>
            <artifactId>annotations</artifactId>
        </dependency>
        <dependency>
            <groupId>software.amazon.awssdk</groupId>
            <artifactId>utils</artifactId>
        </dependency>
        <dependency>
            <groupId>software.amazon.awssdk</groupId>
            <artifactId>utils-lite</artifactId>
        </dependency>
        <dependency>
            <groupId>software.amazon.awssdk</groupId>
            <artifactId>metrics-spi</artifactId>
        </dependency>
        <dependency>
            <groupId>software.amazon.awssdk</groupId>
            <artifactId>json-utils</artifactId>
        </dependency>
        <dependency>
            <groupId>software.amazon.awssdk</groupId>
            <artifactId>endpoints-spi</artifactId>
        </dependency>
        <dependency>
            <groupId>software.amazon.awssdk</groupId>
            <artifactId>profiles</artifactId>
        </dependency>
        <dependency>
            <groupId>software.amazon.awssdk</groupId>
            <artifactId>checksums</artifactId>
        </dependency>
        <dependency>
            <groupId>software.amazon.awssdk</groupId>
            <artifactId>checksums-spi</artifactId>
        </dependency>
        <dependency>
            <groupId>software.amazon.awssdk</groupId>
            <artifactId>retries</artifactId>
        </dependency>
        <dependency>
            <groupId>software.amazon.awssdk</groupId>
            <artifactId>retries-spi</artifactId>
        </dependency>
        <dependency>
            <groupId>software.amazon.awssdk</groupId>
            <artifactId>arns</artifactId>
        </dependency>
        <dependency>
            <groupId>software.amazon.awssdk</groupId>
            <artifactId>crt-core</artifactId>
            <version>${aws.sdk.version}</version>
        </dependency>
        <dependency>
            <groupId>software.amazon.awssdk</groupId>
            <artifactId>third-party-jackson-core</artifactId>
        </dependency>
        <!-- Non-AWS transitive deps -->
        <dependency>
            <groupId>software.amazon.eventstream</groupId>
            <artifactId>eventstream</artifactId>
            <version>1.0.1</version>
        </dependency>
        <dependency>
            <groupId>org.reactivestreams</groupId>
            <artifactId>reactive-streams</artifactId>
            <version>1.0.4</version>
        </dependency>

        <!-- Jackson: embedded in the bundle -->
        <dependency>
            <groupId>com.fasterxml.jackson.core</groupId>
            <artifactId>jackson-core</artifactId>
            <version>${jackson.version}</version>
            <scope>compile</scope>
        </dependency>
        <dependency>
            <groupId>com.fasterxml.jackson.core</groupId>
            <artifactId>jackson-databind</artifactId>
            <version>${jackson.version}</version>
            <scope>compile</scope>
        </dependency>
        <dependency>
            <groupId>com.fasterxml.jackson.core</groupId>
            <artifactId>jackson-annotations</artifactId>
            <version>${jackson.version}</version>
            <scope>compile</scope>
        </dependency>
        <dependency>
            <groupId>com.fasterxml.jackson.dataformat</groupId>
            <artifactId>jackson-dataformat-cbor</artifactId>
            <version>${jackson.version}</version>
            <scope>compile</scope>
        </dependency>

        <!-- Add test deps here if you have them (JUnit, Mockito, etc.) -->
    </dependencies>

    <build>
        <plugins>
            <!-- Compile with Java 17 -->
            <plugin>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.13.0</version>
                <configuration>
                    <source>${maven.compiler.source}</source>
                    <target>${maven.compiler.target}</target>
                </configuration>
            </plugin>

            <!-- Bundle / OSGi manifest generation -->
            <plugin>
                <groupId>org.apache.felix</groupId>
                <artifactId>maven-bundle-plugin</artifactId>
                <version>${maven.bundle.plugin.version}</version>
                <extensions>true</extensions>
                <configuration>
                    <instructions>

                        <!-- Bundle identity -->
                        <Bundle-Name>AWS Bedrock Connector</Bundle-Name>
                        <Bundle-SymbolicName>org.forgerock.openicf.connectors.awsbedrock-connector</Bundle-SymbolicName>
                        <Bundle-Version>${project.version}</Bundle-Version>
                        <Bundle-Description>Connector for AWS Bedrock Agents</Bundle-Description>
                        <Bundle-Vendor>ForgeRock AS</Bundle-Vendor>
                        <Bundle-License>http://opensource.org/licenses/CDDL-1.0</Bundle-License>

                        <!-- Connector bundle metadata -->
                        <ConnectorBundle-FrameworkVersion>1.5</ConnectorBundle-FrameworkVersion>
                        <ConnectorBundle-Name>org.forgerock.openicf.connectors.awsbedrock-connector</ConnectorBundle-Name>
                        <ConnectorBundle-Version>${project.version}</ConnectorBundle-Version>

                        <!-- Implementation metadata -->
                        <Implementation-Build>0</Implementation-Build>
                        <Implementation-Title>AWS Bedrock Connector</Implementation-Title>
                        <Implementation-Vendor>ForgeRock AS</Implementation-Vendor>
                        <Implementation-Vendor-Id>org.forgerock.openicf.connectors</Implementation-Vendor-Id>
                        <Implementation-Version>${project.version}</Implementation-Version>

                        <!-- Java runtime requirement -->
                        <Require-Capability>
                            osgi.ee;filter:="(&amp;(osgi.ee=JavaSE)(version>=17))"
                        </Require-Capability>

                        <!-- Export our connector packages -->
                        <Export-Package>
                            org.forgerock.openicf.connectors.awsbedrock*;version="${project.version}"
                        </Export-Package>

                        <Import-Package>
                            org.identityconnectors.*;version="${openicf.framework.version}",
                            *
                        </Import-Package>

                        <!--
                            Explicit allowlist — only these artifacts are embedded.
                            Embed-Transitive=false ensures nothing else sneaks in.

                            AWS service modules (9):
                              iam, bedrock, bedrockagent, s3, auth, regions,
                              sdk-core, aws-json-protocol, url-connection-client

                            AWS SDK internals (24):
                              aws-core, aws-query-protocol, aws-xml-protocol,
                              protocol-core, http-auth, http-auth-aws, http-auth-spi,
                              http-auth-aws-eventstream, http-client-spi, identity-spi,
                              annotations, utils, utils-lite, metrics-spi, json-utils,
                              endpoints-spi, profiles, checksums, checksums-spi,
                              retries, retries-spi, arns, crt-core,
                              third-party-jackson-core

                            Non-AWS transitive (2):
                              eventstream, reactive-streams

                            Jackson (4):
                              jackson-core, jackson-databind, jackson-annotations,
                              jackson-dataformat-cbor
                        -->
                        <Embed-Dependency>
                            iam,bedrock,bedrockagent,s3,auth,regions,
                            sdk-core,aws-json-protocol,url-connection-client,
                            aws-core,aws-query-protocol,aws-xml-protocol,
                            protocol-core,http-auth,http-auth-aws,http-auth-spi,
                            http-auth-aws-eventstream,http-client-spi,identity-spi,
                            annotations,utils,utils-lite,metrics-spi,json-utils,
                            endpoints-spi,profiles,checksums,checksums-spi,
                            retries,retries-spi,arns,crt-core,
                            third-party-jackson-core,
                            eventstream,reactive-streams,
                            jackson-core,jackson-databind,jackson-annotations,
                            jackson-dataformat-cbor
                        </Embed-Dependency>
                        <Embed-Transitive>false</Embed-Transitive>
                        <Embed-Directory>lib</Embed-Directory>
                    </instructions>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>